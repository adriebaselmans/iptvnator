---
type: fix
area: tv
---

Launching with the `--tv` flag now actually opens the TV shell, instead of a
kiosk-mode window still showing the regular desktop layout. The flag applies
only to that launch and never changes the saved TV-mode startup setting.
